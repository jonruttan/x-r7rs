# known-failures.txt -- the specs this bundle is known to fail.
#
# One "UNIT: TEST" per line, matching the runner's own `FAIL:` lines verbatim.
# Blank lines and #-comments are ignored.
#
# BY NAME, NOT BY COUNT.  A budget of "43 failures" is satisfied by fixing one
# and breaking another, which is the exact event a ratchet exists to catch.
#
# THE LIST MAY ONLY SHRINK.  tests/spec-gate.sh is red when a failure appears
# that is not here, AND when a line here starts passing.
#
# MEASURED ON A QUIET MACHINE, and that is not fussiness.  This suite reported
# 49, 58 and 247 failures on three consecutive runs of the same tree, with
# `Segmentation fault: 11` and 207 batches dying mid-run -- all of it caused by
# an orphaned x-bin left holding a core, not by anything in the bundle.  With
# the machine clear the set is 43, identical across runs.  A number taken while
# something else is running is a number about the machine, so take it twice and
# make sure nothing else is running.
#
# AGAINST x-lang v0.10.0 -- what lang.xon declares and what CI checks out.
#
# THE LIST SHRANK FROM 43 TO 27 WITHOUT A LINE CHANGING UNDER r7rs/, and the
# ratchet is what said so: sixteen recorded failures started passing on the
# platform, which is red here in the "a fix left unrecorded re-authorises the
# failure later" direction.  Fifteen were the error-object and guard group and
# one was a cond-expand body; what remains of that group is a single clause.
#
# The capability was named in this file before it was adopted: the old note
# said the guard shadow works around the engine's frame-depth binding and that
# (base def-global) would let the shadow go.  It has, upstream.

# --- Bytevectors (23) ------------------------------------------------------
#
# R7RS 6.9.  x/bytevector.x is not loaded: the 2024 module was built on FFI
# entry points that no longer exist, and the platform ships Buf now.  Like
# ports in x-r5rs this is a rewrite against a better substrate rather than a
# port, and it has not been done.  utf8->string and string->utf8 are here
# because they are bytevector-typed either side.
bytevector-append: append empty
bytevector-append: append three bytevectors
bytevector-append: append two bytevectors
bytevector-copy!: copy into target
bytevector-copy: copy whole bytevector
bytevector-copy: copy with start
bytevector-copy: copy with start and end
bytevector-length: length of bytevector
bytevector-length: length of empty bytevector
bytevector-u8-ref: ref first element
bytevector-u8-ref: ref last element
bytevector-u8-set!: set element
bytevector: create bytevector
bytevector: empty bytevector
bytevector?: bytevector predicate false
bytevector?: bytevector predicate true
make-bytevector: make-bytevector with default fill
make-bytevector: make-bytevector with fill
make-bytevector: make-bytevector zero length
string->utf8: basic conversion
string->utf8: conversion with range
utf8->string: basic conversion
utf8->string: conversion with range

# --- Error objects and guard (1) -------------------------------------------
#
# R7RS 6.11, and this was fifteen lines longer until v0.9.0.  `error` raising an
# object that carries its message and irritants, and `guard` binding it, both
# work now -- the whole error-object surface and every guard case except the one
# below.
#
# WHAT IS LEFT IS ONE CLAUSE, not a remnant of the group: `else` in a guard's
# cond clauses.  Every other clause form passes, including the error-object
# clause and the always-matching predicate, which puts this in the clause
# dispatcher rather than in error objects at all.  It is the last line of a
# section whose reason for existing has gone.
guard with cond clauses: guard with else clause

# --- Numerics (3) ----------------------------------------------------------
#
# float arithmetic and expt are exactness questions of the kind x-r5rs settled
# against R5RS 6.2.5 -- exact where the argument is exact and the value is
# exactly representable.  The same treatment would fix these.
#
# cond-expand was the fourth here and passes now; the feature list this bundle
# did not publish is no longer what that case needed.
expt: expt with float base
float arithmetic: float addition
float arithmetic: float multiplication
